# Digital Content Safety Filter

YOLOv8 model to classify content as safe / unsafe.

## Setup
pip install -r requirements.txt

## Run
python main.py

## Notes
- Uses custom dataset
- Model weights shared separately
